#include "ogl/CTextureLoader.hpp"

namespace ogl {
  
  ////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
  
  PTextureStream CDDSTextureLoader::load(const sys::CString& file) {
    // @todo: open file
    
    CTextureStream* pStream {new CTextureStream};
    
    
    return pStream;
  }
}

#version 420 core

//uniform struct {
//  vec3 vPosition;
//} u_oLight;

//in vec3 v_vPosition;
//out float f_fDepth;

void main(void) {
  //vec3 vL = v_vPosition - u_oLight.vPosition; // light to fragment
  //f_fDepth = length(vL);
  
  //depth = 1.0 - (1.0 - depth) * 25.0;
  //f_vColor = vec4(depth);
}
